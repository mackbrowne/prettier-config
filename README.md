# `@mackbrowne/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @mackbrowne/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@mackbrowne/prettier-config"
}
```
